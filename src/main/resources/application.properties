# database connection credentials

database.url=jdbc:postgresql://localhost:5432/flights_repository
database.user=postgres
database.password=root

# database connection pool size

database.connection_pool.size=5
